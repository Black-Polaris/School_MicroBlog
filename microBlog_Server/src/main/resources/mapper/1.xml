<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserMapper">
    <resultMap id="UserMapper" type="com.example.entity.User">
        <id property="id" column="user_id"></id>
        <result property="username" column="username"></result>
        <result property="password" column="password"></result>
        <result property="nickname" column="nickname"></result>
        <result property="gender" column="gender"></result>
        <result property="description" column="description"></result>
        <result property="birthDate" column="birth_date"></result>
        <result property="createDate" column="create_date"></result>
        <association property="avatar" javaType="com.example.entity.Avatar">
            <id property="id" column="avatar_id"></id>
            <result property="userId" column="user_id"></result>
            <result property="avatarUrl" column="avatar_url"></result>
            <result property="createDate" column="create_date"></result>
        </association>
    </resultMap>
    <select id="findByUsername" parameterType="String" resultMap="UserMapper">
        select user.*,avatar.avatar_url
        from user, avatar
        where user.avatar_id = avatar.avatar_id and user.username = #{username}
    </select>

</mapper>
